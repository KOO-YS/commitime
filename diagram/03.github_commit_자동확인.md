sequenceDiagram
    actor User as 사용자
    participant App as Flutter App
    participant Provider as GoalProvider
    participant Storage as LocalStorage
    participant GitHub as GitHubService

    Note over User,GitHub: 사용자가 GitHub에 커밋 완료 (외부)
    
    User->>App: 앱 실행 (저녁 11시)
    App->>Provider: loadGoals()
    Provider->>Storage: getGoals()
    Storage-->>Provider: [GitHub 목표]
    Provider->>Storage: getRecordsForDate(오늘)
    Storage-->>Provider: [] (아직 체크 안 됨)
    
    Note over User,App: 홈 화면 표시<br/>GitHub 목표: 미완료
    
    User->>App: 새로고침 (아래로 스와이프)
    App->>Provider: loadGoals()
    Provider->>GitHub: getTodayCommitCount(username)
    GitHub-->>Provider: 5 commits
    
    alt 커밋이 있으면 (5 > 0)
        Provider->>Storage: saveRecord(완료=true, commits=5)
        Storage-->>Provider: 저장 완료
    else 커밋이 없으면
        Provider->>Storage: saveRecord(완료=false, commits=0)
    end
    
    Provider->>Provider: notifyListeners()
    Provider-->>App: 상태 업데이트
    App->>User: UI 업데이트
    Note over App,User: - GitHub 목표 완료 표시<br/>- "5 commits" 표시<br/>- 체크 아이콘
